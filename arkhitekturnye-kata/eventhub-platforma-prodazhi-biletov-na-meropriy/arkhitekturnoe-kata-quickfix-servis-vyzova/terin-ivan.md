---
order: 1
title: Терин Иван (Ticket Service, микросервис по покупке и бронированию билетов)
---

## 1\. 📖 Описание функциональных границ микросервиса

### 1\.1 Диаграмма компонентов архитектуры

[mermaid:./terin-ivan.mermaid:Диаграмма компонентов архитектуры:780px:431px::582px]

### 1\.2 Описание микросервиса

```
Ticket Service (Сервис билетов) — это микросервис, 
управляющий жизненным циклом билета с момента выбора места 
пользователем до момента его успешной покупки или возврата. 
Он является центральным узлом для транзакций, связанных с местами, 
и обеспечивает последовательность данных, чтобы одно и то же место не было продано дважды.

Функциональные границы микросервиса:
-Контроль состояний билетов: Отслеживание и изменение статусов мест (например: Свободно, Забронировано, Оплачено)
-Механизм бронирования: Временная блокировка выбранных билетов за конкретным пользователем на заданный период
-Снятие брони: Автоматический возврат билетов в свободную продажу, если тайм-аут брони истек
-Фиксация факта покупки после успешной оплаты и генерация уникальных идентификаторов билетов
-Оформление возвратов: Изменение статуса билета на "возвращен" и освобождение места для повторной продажи.
```

## 2\. 🧩 Концептуальное проектирование API метода

{% table header="row" %}

---

*  {% colwidth=[144] %}

   Потребители

*  {% colwidth=[147] %}

   Цель

*  {% colwidth=[230] %}

   Задачи

*  {% colwidth=[195] %}

   Входные данные

*  {% colwidth=[239] %}

   Выходные данные

---

*  {% colwidth=[144] %}

   **ФРОНТЕНД**

*  {% colwidth=[147] %}

   Получить список броней

*  {% colwidth=[230] %}

   Запросить список заказов с возможностью отфильтровать их по статусу (забронировано, оплачено, отменено).

*  {% colwidth=[195] %}

   Параметр `status` (фильтр по `BOOKED`, `PAID` или `CANCELLED`).

*  {% colwidth=[239] %}

   Массив объектов брони (содержит ID брони `bookingId` и её статус `status`).

---

*  {% colwidth=[144] %}

   **ФРОНТЕНД**

*  {% colwidth=[147] %}

   Создать бронь билетов

*  {% colwidth=[230] %}

   Зафиксировать за пользователем выбранные места на конкретное мероприятие (защита от «бронирования воздуха»).

*  {% colwidth=[195] %}

   Идентификатор мероприятия (`eventId`), массив идентификаторов выбранных мест (`seatIds`).

*  {% colwidth=[239] %}

   Объект с уникальным номером созданной брони (`bookingId`) и её статусом (`status`).

---

*  {% colwidth=[144] %}

   **ФРОНТЕНД**

*  {% colwidth=[147] %}

   Отменить бронь

*  {% colwidth=[230] %}

   Снять блокировку с мест и аннулировать заказ по инициативе пользователя до оплаты.

*  {% colwidth=[195] %}

   Идентификатор брони (`bookingId`) передается прямо в пути URL.

*  {% colwidth=[239] %}

   Подтверждение успешной отмены (код 200).

---

*  {% colwidth=[144] %}

   **Микросервис** *(Payment Service)*

*  {% colwidth=[147] %}

   Изменить статус (оплатить)

*  {% colwidth=[230] %}

   Обновить статус брони в базе данных после того, как прошла успешная оплата через шлюз.

*  {% colwidth=[195] %}

   Идентификатор брони (`bookingId`) в URL, новый статус (`status`) в теле запроса.

*  {% colwidth=[239] %}

   Подтверждение успешного обновления статуса (код 200)

{% /table %}

## 3\. 🤝 Swagger

[openapi:./_index-3.yaml:true]

### 